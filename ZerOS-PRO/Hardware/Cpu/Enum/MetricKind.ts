/**
 * @module ZerOS.Hardware.Cpu.MetricKind
 * @description 核心指标的封闭取值
 *
 * ---------------------------------------------------------------------------
 * 文件职责
 * ---------------------------------------------------------------------------
 * 只放每个核心要记录的两种指标。
 * 不读取核心，也不改频率。
 *
 * ---------------------------------------------------------------------------
 * 代码组织（严格优先级，自上而下，禁止打乱）
 * ---------------------------------------------------------------------------
 *   1. 指标常量
 */

export namespace ZerOS {
  export namespace Hardware {
    export namespace Cpu {
      /** 该核心当前 Hz。 */
      export const MetricHertz = 0;

      /** 该核心已经完成的命令条数。 */
      export const MetricExecuted = 1;
    }
  }
}

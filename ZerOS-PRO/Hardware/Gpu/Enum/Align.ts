/**
 * @module ZerOS.Hardware.Gpu.Align
 * @description 文本在矩形内的对齐
 *
 * ---------------------------------------------------------------------------
 * 文件职责
 * ---------------------------------------------------------------------------
 * 水平与垂直各三个封闭取值。
 * 不计算坐标。排版时由场景树使用。
 *
 * ---------------------------------------------------------------------------
 * 代码组织（严格优先级，自上而下，禁止打乱）
 * ---------------------------------------------------------------------------
 *   1. 对齐常量
 */

export namespace ZerOS {
  export namespace Hardware {
    export namespace Gpu {
      /** 水平靠左，或垂直靠上。 */
      export const AlignStart = 0;

      /** 水平或垂直居中。余数丢弃。 */
      export const AlignCenter = 1;

      /** 水平靠右，或垂直靠下。 */
      export const AlignEnd = 2;
    }
  }
}

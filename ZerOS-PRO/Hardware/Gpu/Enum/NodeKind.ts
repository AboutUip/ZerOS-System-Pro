/**
 * @module ZerOS.Hardware.Gpu.NodeKind
 * @description 节点种类的封闭取值
 *
 * ---------------------------------------------------------------------------
 * 文件职责
 * ---------------------------------------------------------------------------
 * 只放盒子与文本两种节点。根也是盒子。
 * 不建树，也不画像素。
 *
 * ---------------------------------------------------------------------------
 * 代码组织（严格优先级，自上而下，禁止打乱）
 * ---------------------------------------------------------------------------
 *   1. 种类常量
 */

export namespace ZerOS {
  export namespace Hardware {
    export namespace Gpu {
      /** 盒子。可以有子节点，并可用一种像素填满自己的矩形。 */
      export const NodeKindBox = 1;

      /** 文本。叶子节点，按对齐把字形排进自己的矩形。 */
      export const NodeKindText = 2;
    }
  }
}

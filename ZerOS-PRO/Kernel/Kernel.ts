/**
 * @module ZerOS.Kernel
 * @description 内核层
 *
 * ---------------------------------------------------------------------------
 * 文件职责
 * ---------------------------------------------------------------------------
 * 内核当前不持有硬件，也不调用内存端口。
 * 内存总控在内存自己的门面上，通道在主板上。
 *
 * ---------------------------------------------------------------------------
 * 代码组织（严格优先级，自上而下，禁止打乱）
 * ---------------------------------------------------------------------------
 *   1. Kernel 命名空间
 */

export namespace ZerOS {
  /**
   * 内核。
   * 这一层现在没有字段、没有方法。进程地址和权限以后才放这里。
   */
  export namespace Kernel {}
}

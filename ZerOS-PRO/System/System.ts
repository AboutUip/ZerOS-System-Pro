/**
 * @module ZerOS.System
 * @description 系统层
 *
 * ---------------------------------------------------------------------------
 * 文件职责
 * ---------------------------------------------------------------------------
 * 系统当前不持有硬件，也不持有内核对象。
 * 目录单独存在，用来和硬件、内核分开。
 *
 * ---------------------------------------------------------------------------
 * 代码组织（严格优先级，自上而下，禁止打乱）
 * ---------------------------------------------------------------------------
 *   1. System 命名空间
 */

export namespace ZerOS {
  /**
   * 系统。
   * 这一层现在没有字段、没有方法。
   */
  export namespace System {}
}
